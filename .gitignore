# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*storybook.log
storybook-static

# Generated UI snapshot (tools/design-snapshot.mjs)
design-snapshot/
design-snapshot.zip

# Environment Variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Testing
coverage

**/example_music

# Throwaway Playwright probes used to drive the app while debugging.
# They must live at the repo root so `playwright` resolves, so they are ignored here.
.probe*.mjs
